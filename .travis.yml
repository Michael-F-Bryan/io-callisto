sudo: false
language: node_js
cache: yarn

script:
  - yarn run build
  - yarn run test:unit

before_deploy:
  - yarn run docs

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master